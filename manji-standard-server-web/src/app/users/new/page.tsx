// Scaffolded by mss-protoc-gen — 編集可(アプリ所有)。再生成では上書きされない。

import { UserForm } from "../../../gen/ui/user/UserForm";
import { Page } from "../../../lib/widgets";

export default function NewUserPage() {
  return (<Page title="New User"><UserForm /></Page>);
}
